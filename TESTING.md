# Testing ZabAudioBooker & sending feedback

This is a checklist for putting ZabAudioBooker through its paces, and
a template for reporting what you find.

Everything here refers to `zabaudiobooker.html`. See
[MANUAL.md](MANUAL.md) if you have not opened it yet.

---

## Why this needs your testing

The automated tests cover the parts that can run without a network connection:
53 checks over Markdown parsing, chapter splitting, the WAV encoder, error
handling and the interface. Those all pass.

The PDF path is different, and worth saying so plainly: it **was** tested
end-to-end here. Real PDFs were generated with running heads, folios,
superscript references, footnote blocks and an index, converted, and asserted
against. What that does not cover is real books, which are messier than fixtures
I wrote myself.

**But no audio has ever actually been generated by this code.** The machine it
was built on cannot reach `huggingface.co` or `cdn.jsdelivr.net`, so the entire
model-download-and-speak path is unproven. That is what needs a human.

Specifically unverified:

- Downloading the model at all.
- Whether `kokoro-js` behaves as its documentation claims (the streaming API,
  the options passed to it, the shape of the audio it returns).
- Whether WebGPU is detected and used.
- Whether MP3 encoding works — that library is loaded on demand and has never
  been loaded.
- Whether the voice list refreshes from the model after loading.
- Anything about how it feels to use on a real book.

---

## The test run

Work top to bottom; the early ones are the ones that matter.

### 1. It loads and does the first thing

- [ ] Double-click `zabaudiobooker.html`. The page appears, with no red box.
- [ ] Paste in a few sentences of plain text. The summary line under the box
      shows a word count and an estimated duration.
- [ ] Click **Generate audiobook**. The model download starts and the progress
      bar moves.
- [ ] Audio is produced, a player appears, and **pressing play produces
      recognisable speech**.

**If this last box fails, stop and report it.** Nothing below matters until
audio comes out.

### 2. Markdown handling

Use a real `.md` file — a README, some notes, a chapter of something.

- [ ] Expand **Preview the text that will be spoken** before generating. Read
      it. Does it match what you would want read aloud?
- [ ] Headings are spoken as their text, with no `#`.
- [ ] Code blocks are **not** read out.
- [ ] Links are read as their text, with no web address spoken.
- [ ] Lists sound like separate sentences, not one run-on.
- [ ] Tables, if you have any, are comprehensible.
- [ ] Nothing was dropped that you wanted kept, and nothing was read that you
      wanted skipped.

Point 2 is where your judgement is worth more than any test I can write. The
rules are opinions — tell me where mine are wrong.

### 3. Chapters

- [ ] With **Split at Heading 2**, you get one file per `##` section plus a
      complete book.
- [ ] The chapter titles on each track match the headings.
- [ ] The filenames are sensible (`01-chapter-one.mp3`).
- [ ] **Don't split** gives exactly one file.
- [ ] The complete book plays end to end without gaps or repeats at the joins.

### 4. Formats

- [ ] MP3 generates, downloads and plays in your normal music player.
- [ ] WAV generates, downloads and plays.
- [ ] File sizes look sane (MP3 roughly 1 MB per minute).
- [ ] **Download all as .zip** produces one `.zip` that your OS opens
      normally, containing the complete book and every chapter file, and
      each one plays back correctly after extracting.

### 5. Settings

- [ ] Changing the **Voice** audibly changes the voice.
- [ ] **Speed** `1.25` is faster and still natural.
- [ ] The voice dropdown after the model loads — did the list change? Are the
      entries still readable?
- [ ] Set **Device** to `WASM (CPU)` and generate something short. Slower, but
      it works.
- [ ] Set **Precision** to `q8` and confirm it still works.

### 6. Robustness

- [ ] **Cancel** during a long run stops it promptly.
- [ ] Generating twice in a row works, and the second run reuses the model
      without re-downloading.
- [ ] A long document (30+ minutes of audio) completes without the tab dying.
- [ ] Turn off your Wi-Fi after the model has downloaded once, reload the page,
      and generate. It should still work — that is the whole offline promise.

### 7. PDF ingestion

Use a real PDF you care about. The heuristics were tuned against fixtures I
invented, so this is where they meet reality.

- [ ] Dropping a `.pdf` fills the Markdown box rather than erroring.
- [ ] Headings came out as headings, so chapter splitting still works.
- [ ] The running head is **not** repeated through the text.
- [ ] Page numbers are gone.
- [ ] Footnote markers are gone from mid-sentence, and footnote text is gone.
- [ ] Index and contents pages are gone.
- [ ] Words broken across a line rejoined, no "exam ple".
- [ ] Paragraphs are paragraphs, not one wall of text and not one line each.
- [ ] A scanned PDF gives the OCR message, not empty output or silence.
- [ ] No "Downloaded from ... by guest on ..." stamp anywhere in the text.
- [ ] No mangled words from ligature glyphs, such as "ªnancial" for financial.

Tell me the ratio that matters: roughly how much of the converted text needed
hand-fixing before it was worth listening to. That number decides whether the
heuristics are good enough or need another pass.

---

## What to record as you go

For each problem, the four things that make it fixable:

1. **What you did** — which file, which settings.
2. **What you expected.**
3. **What happened instead** — exact wording of any error.
4. **Your setup** — browser and version, operating system, and whether you have
   a graphics card.

The status line under the Generate button and the numbers in it (audio
produced, elapsed, RTF) are useful — include them for anything speed-related.

## How to capture a useful error

If the page misbehaves, the browser console usually says why.

1. Press **F12** (Mac: **Cmd+Option+I**).
2. Click the **Console** tab.
3. Reproduce the problem.
4. Copy any red lines — right-click, *Copy*, or select and Ctrl+C.

Paste those in verbatim. A screenshot of the console works too. Red text there
is far more useful than a description of the symptom.

---

## Sending it back

**On the pull request** — the most durable place, since it sits with the code:

> https://github.com/Zabin/ZabAudioBooker/pull/1

**Or just tell me here** in Claude Code, in whatever form is convenient — a
list, a paste of console output, or "chapter 3 sounds wrong". I can read the
rough version.

Don't polish it. "The preview looked right but it played nothing and the
console said something about ort-wasm" is a perfectly good bug report.

### Template, if it helps

```
Setup:     Chrome 130 on Windows 11, NVIDIA RTX 4050
Document:  my-notes.md, about 4000 words
Settings:  af_heart, MP3, split at H2, device Auto

Worked:
- Model downloaded (about 3 min), audio generated, speech is clear
- Chapter splitting produced 6 files with the right titles

Didn't work:
- WAV output: clicked Download, file was 0 bytes
- Console said: <paste>

Judgement calls I'd change:
- Tables are read as "Voice: af_heart, Accent: American" which is unbearable
  in a long document; I'd rather skip them by default
- I want the heading pause to be longer
```

That last section is the most valuable one. The bugs I can find by looking
harder; the opinions I cannot.
